mutation {
    createAlbum(input:{name:"First Album"}) {
        id
        name
    }
}

mutation {
    createAlbum(input:{name:"Second Album"}) {
        id
        name
    }
}

query {
    listAlbums {
        items {
            id
            name
        }
    }
}